# КОНТЕКСТ ПРОЄКТУ: Telegram-бот "invest-bot" (BTC/ETH, long-term)

Єдиний документ з усією концепцією та рішеннями — щоб продовжити роботу на будь-якій машині.
Роль: спеціаліст з Telegram-ботів і крипто-стратегій. Відповідати українською.

---

## 1. Що робить бот (головна ідея)

Підказує, **КОЛИ і ЯКИМИ лімітними ордерами** докуповувати BTC та ETH під
довгострокову стратегію (накопичення). Сам аналізує ринок з відкритих джерел.
По команді `/invest` дає або готові ордери, або чесне «зараз почекай».
Сам шле алерти, коли настав хороший момент. Мінімум тексту, без жаргону.

**ВАЖЛИВО:** бот НЕ має ключів від біржі, нічого не торгує і не бачить мій рахунок —
я ставлю/скасовую ордери на Bybit вручну. Єдиний секрет — токен бота.

---

## 2. Зафіксовані рішення

| Параметр | Рішення |
|---|---|
| Призначення | Поради по докупці BTC + ETH (long-term DCA) |
| Режим | **Тільки рекомендація** — ордери виставляю я сам, руками |
| Ключі від біржі | **НЕ використовуються** (ні торгові, ні read-only) — НІКОЛИ не давати |
| Біржа для ордерів | Bybit (ставлю/скасовую вручну) |
| Джерело даних | **CoinGecko** (публічно, без акаунта/ключів) |
| Мова / фреймворк | Python 3.9+, aiogram 3.22 (polling) |
| Хостинг (мета) | Oracle Cloud Always Free, 24/7, як `systemd`-сервіс |
| Монети | тільки **BTC + ETH** |
| Розподіл BTC/ETH | динамічний; **вага монети = її buy score ÷ сума скорів обох** |
| Тип ордерів | «драбинка» з 3 лімітних ордерів нижче ціни, крок за волатильністю |
| Базова сума | $100 (`/invest` без суми = на $100; `/invest 200` = під суму); змінюється `/base` |
| Підтвердження | окрема кнопка на КОЖЕН ордер (✅ поставив / ✖️ пропустити) |
| Відстеження | «Спосіб А» (без ключів, по ринковій ціні) |
| Користувач | тільки я (whitelist по Telegram ID) |
| Місячний бюджет | ~$200-300 максимум |

---

## 3. Приклад відповіді на `/invest`

**Хороший момент** (кожен ордер — окремим повідомленням/рядком зі своїми кнопками
«✅ поставив / ✖️ пропустити», бо грошей може бути лише на 1-2 ордери з 3):
```
✅ Зараз добрий момент вкласти. (на $100)

BTC:
• 0.000201 BTC по $66 450  ($13)   [✅ поставив] [✖️ пропустити]
• 0.000206 BTC по $64 800  ($13)   [✅ поставив] [✖️ пропустити]
• 0.000212 BTC по $62 900  ($13)   [✅ поставив] [✖️ пропустити]

ETH:
• 0.00388 ETH по $3 350  ($13)     [✅ поставив] [✖️ пропустити]
• 0.00405 ETH по $3 210  ($13)     [✅ поставив] [✖️ пропустити]
• 0.00426 ETH по $3 050  ($13)     [✅ поставив] [✖️ пропустити]

⚠️ Не фінансова порада.
```
Підтверджені (✅) ордери пишуться в журнал і відстежуються.

**Поганий момент:**
```
⏳ Зараз дорого — раджу почекати. Я напишу, коли стане вигідно.
```

---

## 4. Логіка движка (стратегія)

Для кожної монети рахуємо прихований **Buy Score (0-100)**. Тобі видно лише
підсумок (дешево / норм / дорого), без цифр і термінів.

Складові score (зважено):
- **Mayer (ціна ÷ ~200-денна середня)** — вага 0.35. Дешевше → вищий бал.
- **Положення ціни в річному діапазоні** — вага 0.20. Ближче до річного дна → вищий бал.
- **RSI(14)** — вага 0.20. Перепроданість (<30) → вищий бал.
- **Fear & Greed Index** — вага 0.25. «Страх» → вищий бал.

(Початково планувалися 200-денна + 200-тижнева середні; 200-тижнева наразі
відключена, бо безкоштовний CoinGecko не дає такої глибини історії — див. п.12.)

**Зони за середнім score обох монет (момент ринку):**
- ≥ 60 → «✅ добрий момент», сума ×1.2, повна драбинка (3 ордери)
- 45-60 → «🟢 норм», сума ×1.0
- 33-45 → «🟡 дорогувато», сума ×0.4, один ордер біля ринку
- < 33 → «⏳ почекати», драбинка не пропонується

**Розподіл BTC/ETH:** вага = score монети ÷ сума скорів (дорожча отримує менше).

**Драбинка:** суму на монету ділимо на 3 ордери нижче ціни; глибина сходинок за
волатильністю (u = середнє денне коливання, обмежене 1.5%-4%): кроки приблизно
`-1·u / -2.2·u / -3.6·u`. Перевіряється мінімальний розмір ордера (MIN_NOTIONAL).

**Принцип «ніколи не зупинятись повністю»:** дорого → купувати мало, дешево →
більше, але рідко по-справжньому нуль. У зоні <33 драбинки немає, але страхує
місячне DCA-нагадування, щоб дисципліна накопичення не ламалась.

---

## 5. Покращення «від трейдера» (узгоджені)

1. **Ніколи не зупинятись повністю** (див. вище).
2. **Тайм-аут спрацювання (14 днів):** якщо підтверджені ордери драбинки так і не
   спрацювали ~14 днів — бот НАГАДУЄ добрати частину **по ринку** (дисципліна DCA).
   Це ОКРЕМА фіча, НЕ те саме, що «чистка застряглих».
3. **Чистка застряглих ордерів:** якщо ордер висить 14+ днів і ціна **втекла вгору**
   (>10% над верхньою сходинкою) — бот пропонує **скасувати**, щоб розморозити кошти.
4. **Розумна оцінка «дешево/дорого»** (Mayer + річний діапазон).
5. **Перевірка мінімального розміру ордера** (MIN_NOTIONAL з .env, ≈$5).

---

## 6. Підтвердження та відстеження (Спосіб А, без ключів)

- Кожен ордер підтверджується ОКРЕМО кнопками (✅ поставив / ✖️ пропустити),
  бо грошей може бути лише на 1-2 ордери з 3.
- Підтверджені ордери пишуться в журнал (SQLite).
- Раз на день бот дивиться, чи ринкова ціна опускалась до ціни ордера → позначає
  «ймовірно спрацював». Жодних ключів від біржі.

---

## 7. Команди

`/start`, `/invest [сума]`, `/status`, `/orders`, `/cleanup`, `/base <сума>`, `/alerts on|off`.

- `/start` — привітання + список команд + **показує мій Telegram ID** (для OWNER_ID).
- `/invest [сума]` — рекомендація (без суми = база $100).
- `/status` — поточний стан (ціни + дешево/норм/дорого + настрій ринку).
- `/orders` — мої збережені активні ордери.
- `/cleanup` — застряглі ордери, які варто скасувати.
- `/base <сума>` — змінити базову суму.
- `/alerts on|off` — увімкнути/вимкнути сповіщення.

---

## 8. Алерти (приходять самі, з ордерами на $100 і кнопками; є антиспам)

- 🔔 **Добрий момент** — середній score ≥ 60.
- 🔴 **Капітуляція** — падіння ≥ 8% за добу.
- 🟢 **Зона історично низьких цін** — Mayer ≤ 1.
- 🟡 **Жадібність** — Fear & Greed ≥ 80 (натяк зафіксувати прибуток).
- 📅 **Місячне нагадування DCA** — якщо за місяць не було докупки.
- ⏳ **Добрати по ринку** — якщо ордери драбинки висять ~14 днів і не спрацювали.
- 🧹 **Чистка застряглих** — скасуй, щоб не морозити кошти.
- ✅ **Спрацювання ордерів** — щоденна перевірка.

Перевірка ринку — щогодини; відстеження ордерів і чистка — раз на день.

---

## 9. Стек і структура

Python 3.9+, aiogram 3.22 (Telegram), httpx (дані), APScheduler (алерти), SQLite (журнал).

```
invest-bot/
├── main.py              # запуск бота + планувальник
├── app/
│   ├── config.py        # налаштування з .env (+ підтримка CA_BUNDLE для корп. проксі)
│   ├── market.py        # дані ринку (CoinGecko, публічно, без ключів) + Fear&Greed
│   ├── indicators.py    # sma, rsi, volatility (чисті функції)
│   ├── engine.py        # buy score, зони, розподіл, драбинка
│   ├── formatting.py    # форматування повідомлень (мінімум тексту)
│   ├── storage.py       # SQLite: налаштування, журнал, стан алертів
│   ├── keyboards.py     # інлайн-кнопки "поставив/пропустити" на кожен ордер
│   ├── handlers.py      # команди Telegram + обробка кнопок
│   ├── alerts.py        # планувальник, відстеження спрацювань, чистка
│   └── service.py       # спільна логіка (gather, recommend)
├── requirements.txt     # aiogram, httpx, APScheduler, python-dotenv
├── .env.example         # шаблон; реальний .env у .gitignore
└── .gitignore
```

---

## 10. Запуск локально

1. `cp .env.example .env` → вписати `TELEGRAM_BOT_TOKEN` і `OWNER_ID`
   (свій Telegram id; також покаже `/start`). `CA_BUNDLE` лишити порожнім.
2. `python -m venv .venv && .venv/bin/pip install -r requirements.txt`
3. `.venv/bin/python main.py`
4. Протестувати `/start`, `/invest`, кнопки ордерів, `/status`.

---

## 11. Хостинг — порівняння

| Варіант | Ціна | Складність деплою | Адмін сервера |
|---|---|---|---|
| **Oracle Always Free (ARM)** | безкоштовно назавжди | середня (VM, SSH, systemd) | так |
| **Fly.io** | ~$2-3/міс (картка обов'язкова) | найпростіша (`fly deploy`) | ні |
| **Hetzner VPS** | ~€4/міс | проста | так |

Не підходять: PythonAnywhere free (блокує вихідні запити), Render free (засинає).
Вимоги: 24/7 + вільний вихід в інтернет (до CoinGecko).

Орієнтир: тільки безкоштовно → Oracle; мінімум мороки за пару доларів → Fly.io;
своя машина дешево → Hetzner.

Нотатки по Oracle: картка для верифікації (без списання), спершу trial $300/30 днів,
далі лишаються тільки Always Free ресурси; інколи «Out of capacity» на ARM —
пробувати інший регіон/час; бажано Upgrade to Pay As You Go (безкоштовно, прибирає
видалення простоюючих інстансів) + Budget alert на $0.01.

---

## 12. Поточний стан і відомі питання

- Дизайн узгоджений; per-order кнопки — основний варіант підтвердження.
- **Джерело даних** переключене на CoinGecko (раніше розглядався Bybit/ccxt).
  Через це сигнал «200-тижнева середня» наразі відключений (CoinGecko не віддає
  потрібну глибину/weekly). Відкрите рішення: лишити CoinGecko чи повернути
  публічні дані біржі з повною історією. Будь-який варіант — лише публічні дані.
- **CoinGecko ліміти** безкоштовного API (можливі 429 при частих запитах) — для
  щогодинних перевірок ок, але варто кешувати відповіді.
- **MIN_NOTIONAL** береться з .env (≈$5), без звірки з правилами біржі.
- **Проксі/сертифікати:** на попередній машині живий тест у Telegram падав через
  корпоративний проксі Cisco Umbrella (TLS-перехоплення,
  "unable to get local issuer certificate"). На ноуті без проксі — лишити
  `CA_BUNDLE` порожнім, і Telegram працює зі звичайними сертифікатами.

> Примітка: у різних сесіях стан коду відрізнявся. Орієнтуватись на цей документ
> як на джерело істини по ДИЗАЙНУ; фактичний стан файлів перевіряти в репозиторії.

---

## 13. TODO-ПЛАН РЕАЛІЗАЦІЇ (виконувати по порядку)

Позначки: `[x]` — вже зроблено в репозиторії, `[ ]` — треба зробити.
Кожен пункт самодостатній: написано ЩО саме і ЗА ЯКОЮ логікою (деталі — розділи вище).

### Етап 0 — Каркас (ГОТОВО)
- [x] Структура проєкту, `requirements.txt`, `.gitignore`, `.env.example`
- [x] `app/config.py` — читання `.env` (TELEGRAM_BOT_TOKEN, OWNER_ID, BASE_AMOUNT,
      RUNGS, MIN_NOTIONAL, STALE_DAYS, пороги алертів)
- [x] `app/indicators.py` — `sma`, `rsi`, `volatility`

> Увага: у config поки НЕМАЄ підтримки CA_BUNDLE — додати при потребі (корп. проксі).

### Етап 1 — Дані
- [ ] `app/market.py`:
  - `fetch_coin(coin)` → CoinGecko `/simple/price` (ціна + `..._24h_change`) і
    `/coins/{id}/market_chart?vs_currency=usd&days=365` (денні `closes` + `ts`).
    Мапа `BTC→bitcoin`, `ETH→ethereum`.
  - `fetch_fng()` → `https://api.alternative.me/fng/?limit=1` (при помилці = 50).
  - Кешувати відповіді (≈10-15 хв), щоб не ловити 429 від CoinGecko.

### Етап 2 — Движок
- [ ] `app/engine.py`:
  - `_coin_score(d, fng)` → зважений buy score: Mayer 0.35 (`_lin(mayer,0.8,2.4)`),
    річне положення 0.20 (`(1-pos)*100`), RSI 0.20 (`_lin(rsi,30,70)`), F&G 0.25 (`100-fng`).
  - Зони за середнім score: ≥60 buy ×1.2 / 45-60 ok ×1.0 / 33-45 pricey ×0.4 / <33 wait.
  - Розподіл BTC/ETH = score ÷ сума скорів.
  - Драбинка: 3 ордери, кроки `-1u/-2.2u/-3.6u`, `u=clamp(vol,0.015,0.04)`;
    pricey = 1 ордер біля ринку; wait = без ордерів; перевірка `MIN_NOTIONAL`.
  - Dataclass-и `Order`, `CoinPlan`, `Recommendation`.

### Етап 3 — Зберігання
- [ ] `app/storage.py` (SQLite): таблиці `settings`, `recommendations`, `orders`, `alert_state`.
  - база/alerts on-off, `save_recommendation`, підтвердження/пропуск ОКРЕМОГО ордера,
    `open_orders`, `mark_filled/cancelled`, `has_buy_this_month`, cooldown алертів.

### Етап 4 — Форматування + кнопки
- [ ] `app/formatting.py` — `fmt_price/fmt_qty/fmt_usd`, `format_recommendation`, `format_status`.
- [ ] `app/keyboards.py` — інлайн-кнопки на КОЖЕН ордер (✅ поставив / ✖️ пропустити),
      `callback_data` несе id ордера/рекомендації.

### Етап 5 — Сервіс + хендлери
- [ ] `app/service.py` — `gather` (дані + F&G), `recommend` (analyze + збереження).
- [ ] `app/handlers.py` — `/start` (+показ Telegram ID), `/invest`, `/status`, `/orders`,
      `/cleanup`, `/base`, `/alerts`; whitelist по `OWNER_ID`; обробка кнопок.

### Етап 6 — Алерти
- [ ] `app/alerts.py` (APScheduler):
  - щогодини: добрий момент / капітуляція (−8% за добу) / глибока вартість (Mayer≤1) /
    жадібність (F&G≥80) — з антиспамом (cooldown через `alert_state`).
  - раз на день: відстеження спрацювань, тайм-аут 14 днів (добрати по ринку),
    чистка застряглих (ціна >10% над верхньою сходинкою), місячне DCA-нагадування.

### Етап 7 — Точка входу
- [ ] `main.py` — ініціалізація `Bot`/`Dispatcher`, `storage.init()`, старт планувальника,
      `dp.start_polling(bot, services=...)`.

### Етап 8 — Запуск і тест (локально)
- [ ] `cp .env.example .env` → вписати `TELEGRAM_BOT_TOKEN` + `OWNER_ID`
- [ ] `python -m venv .venv && .venv/bin/pip install -r requirements.txt`
- [ ] `.venv/bin/python main.py` → перевірити `/start`, `/invest`, кнопки, `/status`, алерти

### Етап 9 — Деплой
- [ ] Обрати хостинг (Oracle Always Free / Fly.io / Hetzner — розділ 11)
- [ ] Налаштувати роботу 24/7 (`systemd` або `fly deploy`)
- [ ] Заллити в GitHub

### Відкрите рішення (вирішити по дорозі)
- [ ] Джерело даних: лишити CoinGecko чи повернути публічні дані біржі з повною
      історією (щоб відновити «200-тижневу середню»). Будь-який варіант — лише публічні дані.

---

## 14. Правила та застереження

- Не створювати summary.md/readme.md — усе пояснювати в чаті. Нові допоміжні файли
  (правила тощо) класти в `.cursor/`. Відповідати українською.
- Бот не має доступу до біржового акаунта і не торгує — лише читає публічні ціни.
- Усі поради — не фінансова консультація; остаточне рішення завжди за мною.
